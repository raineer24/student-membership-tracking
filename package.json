{
  "name": "student-membership-tracking",
  "version": "1.0.0",
  "main": "index.js",
"scripts": {
    "dev": "cd client && npm run build && cd .. && vercel dev",
    "build": "cd client && npm run build",
    "start": "cd client && npm run preview",
    "postinstall": "npx prisma generate && cd client && npm install",
    "lint": "eslint . --ext .js,.jsx",
    "format": "prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "node-cron": "^4.0.0",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "prisma": "^6.7.0"
  }
}
